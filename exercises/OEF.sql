USE employeesdb;
SELECT * FROM jobs;
SELECT JOB_TITLE, MIN_SALARY FROM jobs;
select * FROM jobs ORDER BY MAX_SALARY DESC, MIN_SALARY ASC;
SELECT * FROM employees ORDER BY JOB_ID DESC;
select * from jobs;
